# HTTP Basic Auth credentials for the entire site (gated by proxy.ts).
# Copy this file to .env.local and change the values.
#
# In production (Vercel / wherever), set these in the hosting dashboard's
# environment variables UI — don't commit them.

AUTH_USERNAME=tripon
AUTH_PASSWORD=safasoft2026
